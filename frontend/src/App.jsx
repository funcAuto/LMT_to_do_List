// client/src/App.jsx
import React from 'react';
import TodoList from './components/TodoList';  // No need to add `.jsx` explicitly

const App = () => {
  return (
    <div>
      <TodoList />
    </div>
  );
};

export default App;
